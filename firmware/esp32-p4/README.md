# ESP32-P4 七寸屏展示端

成员 B 当前工程原位置：

```text
/Users/buptniaosuan/Desktop/物联网/ESP32-P4-WIFI6-Touch-LCD-7B-main/examples/ESP-IDF/10_lvgl_demo_v9
```

工程尚未同步到共享仓库。上传时请直接放入本目录，并保留原工程中的 `P4工程启动说明.md`。

最低验收要求：

- 可独立构建和烧录；
- 七寸屏中文显示正常；
- 能读取 `status/report/reminders`；
- 网络失败时保留上一份数据并自动重试；
- 后端恢复后无需重启；
- 后端基础地址集中配置，不散落硬编码；
- 不提交 Wi-Fi 密码或其他凭据。
