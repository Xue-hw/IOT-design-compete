# 当前接口说明

本文件只描述当前比赛主线接口：S3/P4 与后端之间的结构化数据、报告和提醒接口。旧的音视频上传和文件传输路线不作为当前主线。

当前产品主设备只有测试用 EYE 与最终演示用 Cube，二者不要求同时在线。C3/AS7341 只作为共用光照子系统；`focuscube-c3-proxy-01` 和 `c3-as7341-proxy` 暂时用于追踪物理来源，不代表第三个产品节点。下列 `focuscube-s3-01` 仅为旧接口兼容样例，不是已经确定的正式绑定 ID。

## 网络原则

- S3 尽量不大改，以已烧录版本可用的数据导出或上传方式为准。
- 后端提供稳定 HTTP API。
- P4 七寸屏优先通过 HTTP 拉取后端状态、报告和提醒。
- Web 看板使用同一组 API，避免为 P4 单独写一套数据格式。

## 后端 API

| 方法 | 路径 | 调用方 | 用途 |
|---|---|---|---|
| POST | `/api/v1/telemetry` | S3/P4/replay 脚本 | 上传结构化 telemetry |
| GET | `/api/v1/status?device_id=` | Web/P4 | 获取当前选中数据源的最新状态 |
| GET | `/api/v1/report/daily?device_id=&date=` | Web/P4 | 获取大模型日报复盘 |
| GET | `/api/v1/reminders?device_id=&since=` | S3/P4/Web | 获取提醒列表 |
| GET | `/api/v1/timeseries?device_id=&date=&metric=` | Web | 获取曲线数据 |
| GET/PUT | `/api/v1/config?device_id=` | Web/调试 | 获取或修改配置 |

## telemetry 示例

```json
{
  "device_id": "focuscube-s3-01",
  "source": "s3",
  "ts": 1718000000,
  "light": {
    "lux": 235.6,
    "label": "suitable"
  },
  "imu": {
    "face": 2,
    "mode": "focus",
    "activity": 0.32
  },
  "focus": {
    "state": "running",
    "remaining_s": 940,
    "session_count": 3
  },
  "power": {
    "battery_pct": 78,
    "charging": false
  }
}
```

## status 示例

```json
{
  "ok": true,
  "now": 1718000123,
  "devices": [
    {
      "device_id": "focuscube-s3-01",
      "source": "s3",
      "product_node": true,
      "device_role": "primary_device",
      "physical_source": "s3",
      "online": true,
      "last_seen": 1718000100,
      "summary": "专注中，光照适宜"
    }
  ]
}
```

## report 示例

```json
{
  "device_id": "focuscube-s3-01",
  "date": "2026-07-05",
  "report_text": "今天上午光照整体适宜，专注时长较稳定。下午有一段光线偏暗，建议调整台灯角度。",
  "metrics": {
    "focus_minutes": 85,
    "pomodoro_count": 3,
    "avg_lux": 236,
    "suitable_light_ratio": 0.78
  },
  "suggestions": [
    "下午学习时保持桌面照度在 300 lux 左右。",
    "每完成 25 分钟专注后休息 5 分钟。"
  ]
}
```

## reminders 示例

```json
[
  {
    "id": "r-001",
    "type": "too_dim",
    "text": "当前光线偏暗，建议打开台灯。",
    "priority": 2,
    "ttl_s": 120
  }
]
```

## P4 七寸屏建议页面

P4 屏幕优先实现 3 个页面或 3 个轮播区：

- 设备状态：S3 在线状态、当前光照、专注状态。
- 大模型复盘：`report_text` 大字号展示。
- 提醒列表：最新 1-3 条提醒。

如果时间紧，先做单页大字体展示，不做复杂菜单。
