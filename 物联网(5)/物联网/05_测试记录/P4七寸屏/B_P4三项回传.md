# B：P4 三项回传

更新时间：2026-07-14

## 1. P4 工程位置

工程目录：

`04_技术实现/P4七寸屏工程`

工程启动说明：

`04_技术实现/P4七寸屏工程/P4工程启动说明.md`

当前固件已完成编译和烧录，目标为 Waveshare ESP32-P4-WIFI6-Touch-LCD-7B，使用 ESP-IDF v6.0.2。

## 2. 七寸屏固定文字照片

屏幕固定中文、中文字体和标题乱码均已修复，现场已确认当前画面无乱码。

最终验收照片：待附一张当前固件运行画面的完整照片。照片需同时拍到以下内容：

- `FocusCube P4 Display`
- `S3 状态`
- `AI 每日复盘`
- `提醒`
- 右上角状态徽标
- 左下角网络状态

修复前照片不能作为最终验收照片，因为其中仍有少量标题绘制异常。

## 3. 能否访问 `/api/v1/status`

结论分为历史验证和当前状态：

- 历史验证：P4 已成功连接 `BUPT-portal`，获得局域网地址，并多次成功请求 `GET http://10.129.90.92:8000/api/v1/status`；屏幕曾显示后端真实 JSON 数据。
- 当前实测：2026-07-14 再次使用电脑请求该接口，TCP 可以连接，但 HTTP 6 秒内无响应，`curl` 返回退出码 `28`。P4 串口也报告 `ESP_ERR_HTTP_CONNECT` 或 `ESP_ERR_HTTP_EAGAIN`。
- 判断：当前阻塞点在 `10.129.90.92:8000` 后端服务，不是 P4、中文显示或校园网认证。请后端负责人检查或重启服务。
- 恢复机制：P4 会保留上次成功内容并自动重试；后端恢复后无需重启 P4，应自动切回 `LIVE API`。

## 可直接发给 A 的回传消息

> B 回传：P4 工程位于 `04_技术实现/P4七寸屏工程`，启动说明为工程内 `P4工程启动说明.md`。七寸屏固定中文和标题乱码已经修复，当前现场确认无乱码，最终完整屏幕照片随后附上。P4 此前已成功访问 `/api/v1/status` 并显示真实 JSON；但当前 `10.129.90.92:8000` 端口可连接、HTTP 请求持续超时，电脑和 P4 结果一致，请检查或重启后端。后端恢复后 P4 会自动继续刷新，无需重启。
