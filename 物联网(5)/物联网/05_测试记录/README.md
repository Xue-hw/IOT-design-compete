# 测试记录索引

测试证据按设备归档，避免源码、运行产物和验收记录混放。

| 目录 | 内容 |
|---|---|
| `S3_EYE` | EYE/S3 功能验收、校园网验证、SD、摄像头和历史构建记录 |
| `P4七寸屏` | P4 联调、屏幕展示与接口回传记录 |

`S3_EYE/固件备份` 只保存本机恢复用 Flash 镜像说明。`.bin` 镜像体积大且包含设备现场状态，不进入 Git；其文件名和 SHA-256 记录在对应上板验证文档中。
