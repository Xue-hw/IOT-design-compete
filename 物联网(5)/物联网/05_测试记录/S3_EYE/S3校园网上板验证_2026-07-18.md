# ESP32-S3 北邮校园网上板验证记录

## 设备

- 芯片：ESP32-S3 (QFN56, revision v0.2)
- Flash：8 MB
- PSRAM：8 MB
- MAC：`e0:72:a1:af:99:40`
- 串口：`/dev/cu.usbmodem11201`

## 验证结果

- ESP-IDF 6.0.2 的 `esp32s3` 目标编译成功。
- 固件烧录成功，写入后校验通过。
- 成功连接开放网络 `BUPT-portal`。
- DHCP 获得地址 `10.129.185.233/16`，网关 `10.129.0.1`。
- 校园网探测返回已认证，串口日志为
  `Campus network is already authenticated`。
- 认证状态每 300 秒自动复检。

## 现场修复

首版固件在获得 IP 后进入 HTTP 门户请求时发生主任务栈溢出。
已将门户认证循环移入独立 8 KB FreeRTOS 任务，重新编译、烧录后
连网与认证均成功，未再出现栈溢出。

## 恢复点

烧录前的完整 8 MB Flash 备份：

`05_测试记录/S3_EYE/固件备份/ESP32-S3_e072a1af9940_2026-07-18_full_8MB.bin`

SHA-256：

`e95fbea212729ee1b513a73af2a4a7b66d40cbfdf102c700b30c73f511fcf276`
