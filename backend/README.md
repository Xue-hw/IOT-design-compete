# 后端与大模型服务

成员 C 的后端已完成核心接口联调，但源码尚未同步到本仓库。

上传时至少应包含：

- 依赖清单和运行环境；
- `.env.example`，不得提交真实 API Key；
- 启动命令；
- telemetry 数据校验；
- 日报生成与刷新机制；
- 大模型失败兜底；
- `status/report/reminders/timeseries` 接口测试。

局域网 IP 是动态配置，不应写死在客户端或文档之外的多个位置。
